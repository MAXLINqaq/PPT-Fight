using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class weaponBStatus : Weapon
{
    // Start is called before the first frame update
    void Start()
    {
        totalWeight = 24;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
